<div class="requruit-block">
  <div class="signup-group">
    <h3 class=title-form>【採用担当者様 専用ログインフォーム】</h3>
    <h4 class="description-form">下記の「専用ログイン」ボタンを押していただければ、<br>すぐに専用アカウントでログインが可能です。</h4>
    <%= form_for(:session, url: login_path, local: true) do |f| %>
      <div class="field"> 
        <%= f.label :email, "メールアドレス" %>
        <%= f.email_field :email, class: "form-control", id: "user_email", value: 'saiyou@example.com' %>
      </div>
      <div class="field"> 
        <%= f.label :password, "パスワード" %>
        <%= f.password_field :password, class: "form-control", id: "user_password", value: 'password' %>
      </div>
      <%= f.label :remember_me, class: "checkbox inline" do %>
        <%= f.check_box :remember_me %>
        <span>ログインしたままにする</span>
      <% end %>
      <div class="actions">
        <%= f.submit "専用ログイン", class: "btn btn-primary btn-requruit" %>
      </div>
    <% end %>
    <div class="recruit-account">
      <p>＜採用担当者様 専用アカウント＞</p>
      <p>メールアドレス：saiyou@example.com</p>
      <p>パスワード：password</p>
      <p>※フォームに予め入力済です</p>
    </div>
  </div>
</div>